<h1>Yo jobs</h1>

<% @jobs.each do |job| %>
  <div class="job">
    <h2><%= link_to job.title, job.url %></h2>
    <p><%= job.company %></p>
  </div>
  <p><%= paginate @jobs %></p>
<% end %>

<p><%= link_to "Add a job", new_job_path %></p>
